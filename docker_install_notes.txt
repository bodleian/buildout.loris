Install docker ubuntu 12:

http://docs.docker.com/installation/ubuntulinux/

Ubuntu 14 install doesn't seem to work.



requirements:

-	Developmental changes to Loris at Princeton to ease auto-deployment and any subsequent build development required to implement those changes
-	IIP/Loris builds deployed to development servers
-	Public exposure of IIP/Loris tile servers to validation
-	Proper functional and unit tests for IIP and Loris to be setup (selenium and newly developed or already available tests)
-	Jenkins CI (Travis CI is currently available and running) setup complete and running above tests
-	Jenkins CI and vm-slave deployed to production server for proper use
-	Full documentation in github for builds (mostly complete) and confluence for CI build (incomplete)
-	external kakadu source put onto a server for auto deployment into repositories