[buildout]

parts +=

extensions =
	mr.developer

extends =
	buildouts/nginx.cfg
	buildouts/vhosts.cfg
	versions.cfg

auto-checkout +=
	
dollar = $

[template]
recipe = collective.recipe.template
input = conf/crondump.txt
output = ${buildout:directory}/bin/crondump.txt

[cmd]
recipe = collective.recipe.cmd
on_install = true
cmds =  

[sources]
usered = git gitlab@source.bodleian.ox.ac.uk:mdb1/usered.git egg=false